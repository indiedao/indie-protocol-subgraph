{
  "name": "indie-protocol-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service indiedao/indie-protocol-subgraph",
    "deploy-test": "graph deploy --product hosted-service seanconnollydev/indie-protocol-subgraph-test",
    "create-local": "graph create --node http://localhost:8020/ indiedao/indie-protocol-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ indiedao/indie-protocol-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 indiedao/indie-protocol-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
